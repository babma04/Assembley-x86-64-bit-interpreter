# Support module

This module contains type definitions that abstract each section of a asm file into a custom python hash table / union type. Also provides file level operations to read, write, conversion and opening for the pre loading phase of the parser.

## Storage

(TODO)